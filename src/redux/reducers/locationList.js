import {} from "../actions";
import { initialState } from "../store";

const locationListReducer = (state = initialState.locationList, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default locationListReducer;
